$strDir="C:\temp\test1\"; Get-ChildItem $strDir | Remove-Item -Force; 1..200 | %{$strPath="$strDir$_" + ".txt"; "something" | Out-File -FilePath $strPath -Encoding ASCII -Force}; Measure-Command {1..101 | %{$strPath="$strDir$_" + ".txt"; $strNewPath="$strPath" + ".chng"; "changed" | Out-File -Append -FilePath $strPath -Encoding ASCII; Rename-Item -Path $strPath -NewName $strNewPath}}
